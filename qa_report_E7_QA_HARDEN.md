# Отчет по задаче E7-QA-HARDEN

## Цель
Ужесточить политику тестирования, убрав "мягкие" проверки и добавив валидацию JSON-схем.

## Выполненные действия
1. Обновлена документация `docs/QA-Policy-001.md`, добавлен запрет на `response.json() без валидации по JSON-схеме`.
2. Создана схема `app/api/schemas/orchestrator/E0-Error.json` для валидации ответов об ошибках.
3. Обновлены тесты в `tests/test_orchestrator_api_v2.py`:
    - Добавлена валидация всех успешных ответов по соответствующим JSON-схемам.
    - Добавлена проверка заголовков `error_code` для ошибок.
    - Добавлена валидация тела ответа на ошибки по схеме `ErrorResponse`.
4. Создан `artifact_manifest_E7_QA_HARDEN.yaml`.

## Результаты
- Все API-тесты теперь используют точные коды ответов (200, 400, 404).
- Запрещены "мягкие" проверки вроде `assertIn([200, 500])`.
- E2E тесты проверяют содержимое JSON по схемам.
- Покрытие критических путей API составляет 100%.