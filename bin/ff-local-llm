#!/usr/bin/env bash
set -euo pipefail

# Delegate to project venv python script
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PY_BIN="/opt/feature-factory/.venv/bin/python3"
if [[ ! -x "$PY_BIN" ]]; then
  PY_BIN="python3"
fi
exec "$PY_BIN" "$SCRIPT_DIR/ff-local-llm.py" "$@"

