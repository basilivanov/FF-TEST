# Дневные лимиты токенов для ролей
# Формат: роль: лимит_токенов

Architect: 999999
Dev: 1400
QA: 800
Scribe: 800
# Для диалогов мейнтейнера потребуется много токенов (контекст + уточнения).
# Поднимем лимит для исключения ложных срабатываний "ресурсы заняты" в MVP.
Maintainer: 300000
