model_list: []  # LiteLLM disabled: agents use OAuth via CLI providers

router_settings:
  routing_strategy: "leastbusy"
  default_deployment: ""

litellm_settings:
  drop_params: true
  request_timeout: 30

environment_variables: {}
