# Patterns — Frontend Tech Stack (React + Vite)

- React + TypeScript, Vite, React Query, Testing Library + Jest, Playwright.
- Доступные роуты: `/admin`, `/admin/features`, `/admin/features/:id`, `/admin/tasks/:id`.
- Источники данных: features_list, feature_graph, task_get.
- Развёртывание: `ff-ui-build-nosudo`, `ff-ui-deploy-safe-nosudo --smoke`.

