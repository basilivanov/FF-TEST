```yaml
# artifact_manifest для задачи E4-BACKLOG-LOOP-RESUME-QA
- tests/test_resume_after_restart.py:
    version: "1.0"
    changes:
      - "Создан тест для проверки возобновления работы графа после перезапуска"
      - "Проверяет, что API не возвращает 500 при повторном запуске фичи"
      - "Проверяет, что фича в итоге получает статус DONE"
```